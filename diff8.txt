commit c394c61eae00488400714af0ad71a1f65267da6a
Author: Vaibhav <vaibhav@example.com>
Date:   Thu Jun 25 10:57:38 2026 +0530

    Implement and connect all 6 Orders sub-tab reports to database

diff --git a/reports.html b/reports.html
index 08723e9..d66bbef 100644
--- a/reports.html
+++ b/reports.html
@@ -1282,34 +1282,214 @@
                         }
                     });
                     rows = newRows.sort((a, b) => a[0].localeCompare(b[0]));
-                } else if (name.includes('Tax Information') || name.includes('GST')) {
-                    const res = await fetch(`${API_BASE}/invoices`);
+                } else if (name === 'Client, Product wise Invoiced Order Job Summary') {
+                    const res = await fetch(`${API_BASE}/orders`);
+                    const allOrders = await res.json();
+                    const dataFiltered = allOrders.filter(o => o.invoiceId);
+                    const groups = {};
+                    dataFiltered.forEach(o => {
+                        const clientName = o.client ? o.client.name : 'Unknown';
+                        if (o.jobs && o.jobs.length > 0) {
+                            o.jobs.forEach(j => {
+                                const type = j.productType || 'Unknown';
+                                const nameVal = j.productName || '-';
+                                const key = clientName + '||' + type + '||' + nameVal;
+                                if (!groups[key]) {
+                                    groups[key] = { clientName, type, nameVal, units: 0, amount: 0 };
+                                }
+                                groups[key].units += (j.units || 0);
+                                groups[key].amount += (j.totalAmount || 0);
+                            });
+                        } else {
+                            const type = o.productType || 'Unknown';
+                            const nameVal = o.productName || '-';
+                            const key = clientName + '||' + type + '||' + nameVal;
+                            if (!groups[key]) {
+                                groups[key] = { clientName, type, nameVal, units: 0, amount: 0 };
+                            }
+                            groups[key].units += (o.units || 0);
+                            groups[key].amount += (o.netAmount || o.totalAmount || 0);
+                        }
+                    });
+                    headers = ['Client', 'Product Type', 'Product Name', 'Total Units', 'Total Amount'];
+                    rows = Object.values(groups).map(g => [
+                        g.clientName,
+                        g.type,
+                        g.nameVal,
+                        g.units,
+                        g.amount.toFixed(2)
+                    ]).sort((a, b) => a[0].localeCompare(b[0]) || a[1].localeCompare(b[1]) || a[2].localeCompare(b[2]));
+                } else if (name === 'Orders Jobs- by Due Date') {
+                    const res = await fetch(`${API_BASE}/orders`);
                     data = await res.json();
-                    headers = ['Invoice #', 'Client', 'Gross', 'Tax', 'Net'];
-                    rows = data.map(inv => [
-                        inv.invoiceNumber,
-                        inv.client ? inv.client.name : 'Unknown',
-                        (inv.grossAmount || 0).toFixed(2),
-                        (inv.taxAmount || 0).toFixed(2),
-                        (inv.netAmount || 0).toFixed(2)
-                    ]);
-                } else if (name.includes('by ProductType')) {
+                    headers = ['Due Date', 'Trial Date', 'Order #', 'Client', 'Patient', 'Product', 'Teeth', 'Units', 'Shade', 'Amount', 'Status'];
+                    let newRows = [];
+                    data.filter(o => o.dueDate)
+                        .sort((a, b) => new Date(a.dueDate) - new Date(b.dueDate))
+                        .forEach(o => {
+                            if (o.jobs && o.jobs.length > 0) {
+                                o.jobs.forEach(j => {
+                                    newRows.push([
+                                        new Date(o.dueDate).toLocaleDateString('en-GB'),
+                                        o.trialDate ? new Date(o.trialDate).toLocaleDateString('en-GB') : '-',
+                                        o.orderNumber || o.id,
+                                        o.client ? o.client.name : 'Unknown',
+                                        o.patientName || '-',
+                                        j.productName || j.productType || '-',
+                                        createTeethGrid(j.teethSelection),
+                                        j.units || '1',
+                                        [j.shade1, j.shade2, j.shade3].filter(s => s).join('/') || '-',
+                                        (j.totalAmount || 0).toFixed(2),
+                                        o.status
+                                    ]);
+                                });
+                            } else {
+                                newRows.push([
+                                    new Date(o.dueDate).toLocaleDateString('en-GB'),
+                                    o.trialDate ? new Date(o.trialDate).toLocaleDateString('en-GB') : '-',
+                                    o.orderNumber || o.id,
+                                    o.client ? o.client.name : 'Unknown',
+                                    o.patientName || '-',
+                                    o.productName || o.productType || '-',
+                                    createTeethGrid(o.teethSelection),
+                                    o.units || '1',
+                                    [o.shade1, o.shade2, o.shade3].filter(s => s).join('/') || '-',
+                                    (o.netAmount || o.totalAmount || 0).toFixed(2),
+                                    o.status
+                                ]);
+                            }
+                        });
+                    rows = newRows;
+                } else if (name === 'Orders Jobs- by ProductType') {
                     const res = await fetch(`${API_BASE}/orders`);
                     const orders = await res.json();
                     const typeData = {};
                     orders.forEach(o => {
+                        const typesInOrder = new Set();
                         if (o.jobs && o.jobs.length > 0) {
                             o.jobs.forEach(j => {
                                 const type = j.productType || 'Unknown';
-                                typeData[type] = (typeData[type] || 0) + 1;
+                                typesInOrder.add(type);
+                                if (!typeData[type]) {
+                                    typeData[type] = { ordersCount: 0, units: 0, amount: 0 };
+                                }
+                                typeData[type].units += (j.units || 0);
+                                typeData[type].amount += (j.totalAmount || 0);
                             });
                         } else {
                             const type = o.productType || 'Unknown';
-                            typeData[type] = (typeData[type] || 0) + 1;
+                            typesInOrder.add(type);
+                            if (!typeData[type]) {
+                                typeData[type] = { ordersCount: 0, units: 0, amount: 0 };
+                            }
+                            typeData[type].units += (o.units || 0);
+                            typeData[type].amount += (o.netAmount || o.totalAmount || 0);
                         }
+                        typesInOrder.forEach(type => {
+                            typeData[type].ordersCount += 1;
+                        });
                     });
-                    headers = ['Product Type', 'Order Count'];
-                    rows = Object.entries(typeData).map(([type, count]) => [type, count]);
+                    headers = ['Product Type', 'Total Orders', 'Total Units', 'Total Amount'];
+                    rows = Object.entries(typeData).map(([type, stats]) => [
+                        type,
+                        stats.ordersCount,
+                        stats.units,
+                        stats.amount.toFixed(2)
+                    ]).sort((a, b) => a[0].localeCompare(b[0]));
+                } else if (name === 'Orders Jobs- by ProductType,Product - Summary') {
+                    const res = await fetch(`${API_BASE}/orders`);
+                    const orders = await res.json();
+                    const typeProdData = {};
+                    orders.forEach(o => {
+                        const keysInOrder = new Set();
+                        if (o.jobs && o.jobs.length > 0) {
+                            o.jobs.forEach(j => {
+                                const type = j.productType || 'Unknown';
+                                const nameVal = j.productName || '-';
+                                const key = type + '||' + nameVal;
+                                keysInOrder.add(key);
+                                if (!typeProdData[key]) {
+                                    typeProdData[key] = { type, nameVal, ordersCount: 0, units: 0, amount: 0 };
+                                }
+                                typeProdData[key].units += (j.units || 0);
+                                typeProdData[key].amount += (j.totalAmount || 0);
+                            });
+                        } else {
+                            const type = o.productType || 'Unknown';
+                            const nameVal = o.productName || '-';
+                            const key = type + '||' + nameVal;
+                            keysInOrder.add(key);
+                            if (!typeProdData[key]) {
+                                typeProdData[key] = { type, nameVal, ordersCount: 0, units: 0, amount: 0 };
+                            }
+                            typeProdData[key].units += (o.units || 0);
+                            typeProdData[key].amount += (o.netAmount || o.totalAmount || 0);
+                        }
+                        keysInOrder.forEach(key => {
+                            typeProdData[key].ordersCount += 1;
+                        });
+                    });
+                    headers = ['Product Type', 'Product Name', 'Total Orders', 'Total Units', 'Total Amount'];
+                    rows = Object.values(typeProdData).map(v => [
+                        v.type,
+                        v.nameVal,
+                        v.ordersCount,
+                        v.units,
+                        v.amount.toFixed(2)
+                    ]).sort((a, b) => a[0].localeCompare(b[0]) || a[1].localeCompare(b[1]));
+                } else if (name.includes('Shipped Orders Jobs- by ProductType')) {
+                    const res = await fetch(`${API_BASE}/orders`);
+                    const orders = await res.json();
+                    const shippedOrders = orders.filter(o => ['Dispatched', 'Delivered'].includes(o.shippingStatus));
+                    const typeProdData = {};
+                    shippedOrders.forEach(o => {
+                        const keysInOrder = new Set();
+                        if (o.jobs && o.jobs.length > 0) {
+                            o.jobs.forEach(j => {
+                                const type = j.productType || 'Unknown';
+                                const nameVal = j.productName || '-';
+                                const key = type + '||' + nameVal;
+                                keysInOrder.add(key);
+                                if (!typeProdData[key]) {
+                                    typeProdData[key] = { type, nameVal, ordersCount: 0, units: 0, amount: 0 };
+                                }
+                                typeProdData[key].units += (j.units || 0);
+                                typeProdData[key].amount += (j.totalAmount || 0);
+                            });
+                        } else {
+                            const type = o.productType || 'Unknown';
+                            const nameVal = o.productName || '-';
+                            const key = type + '||' + nameVal;
+                            keysInOrder.add(key);
+                            if (!typeProdData[key]) {
+                                typeProdData[key] = { type, nameVal, ordersCount: 0, units: 0, amount: 0 };
+                            }
+                            typeProdData[key].units += (o.units || 0);
+                            typeProdData[key].amount += (o.netAmount || o.totalAmount || 0);
+                        }
+                        keysInOrder.forEach(key => {
+                            typeProdData[key].ordersCount += 1;
+                        });
+                    });
+                    headers = ['Product Type', 'Product Name', 'Total Orders', 'Total Units', 'Total Amount'];
+                    rows = Object.values(typeProdData).map(v => [
+                        v.type,
+                        v.nameVal,
+                        v.ordersCount,
+                        v.units,
+                        v.amount.toFixed(2)
+                    ]).sort((a, b) => a[0].localeCompare(b[0]) || a[1].localeCompare(b[1]));
+                } else if (name.includes('Tax Information') || name.includes('GST')) {
+                    const res = await fetch(`${API_BASE}/invoices`);
+                    data = await res.json();
+                    headers = ['Invoice #', 'Client', 'Gross', 'Tax', 'Net'];
+                    rows = data.map(inv => [
+                        inv.invoiceNumber,
+                        inv.client ? inv.client.name : 'Unknown',
+                        (inv.grossAmount || 0).toFixed(2),
+                        (inv.taxAmount || 0).toFixed(2),
+                        (inv.netAmount || 0).toFixed(2)
+                    ]);
                 } else if (name.includes('Daily Order Activity')) {
                     const res = await fetch(`${API_BASE}/orders`);
                     const orders = await res.json();
